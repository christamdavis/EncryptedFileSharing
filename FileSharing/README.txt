I adjusted the client server code to handle file upload to server, and download from server.
The instructions are clear once you compile, putting the commands here in case you need them.

commands:
g++ client.cpp -o client
./client

g++ server.cpp -o server
./server
